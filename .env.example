# Copy this file to .env and fill in the values.
#   cp .env.example .env

# ---------------------------------------------------------------------------
# Required
# ---------------------------------------------------------------------------

# Docker image for the MedDSAgent backend.
# Pull or build it separately, then reference it here.
BACKEND_IMAGE=meddsagent:latest

# ---------------------------------------------------------------------------
# Optional — networking
# ---------------------------------------------------------------------------

# Host port the app (frontend) is served on. Default: 8000
# APP_PORT=8000

# ---------------------------------------------------------------------------
# Optional — heavy build dependencies
# Set to "true" before running "docker compose build" to include them.
# These are excluded by default to keep the image small.
#
#   R         ~50 MB  — needed for R-language agent sessions
#   Docling   ~3 GB   — needed for PDF/document ingestion (pulls PyTorch)
# ---------------------------------------------------------------------------
# INSTALL_R=false
# INSTALL_DOCLING=false
